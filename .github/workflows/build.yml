name: Publish Docker
on: [push]

# TODO
# on:
#   pull_request:
#     branches:
#       - master
#       - releases/*
#   push:
#     branches:
#       - master
#       - releases/*

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Publish to Registry
      uses: HurricanKai/Publish-Docker-Github-Action@master
      with:
        name: docker.pkg.github.com/xplorfin/docker-protoc/docker-protoc
        username: ${{ secrets.GITHUB_DOCKER_USERNAME }}
        password: ${{ secrets. GITHUB_OSS_TOKEN }}
        registry: docker.pkg.github.com
